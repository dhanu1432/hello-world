You’re absolutely right, and I apologize for the confusion. In Spring AI 1.0.0-M6, the `ChatClient.Builder` class does not have a `defaultChatModel` method. My previous responses incorrectly assumed this method existed based on older versions or a misinterpretation of the API. Let’s correct this by aligning with the actual API in Spring AI 1.0.0-M6.

In Spring AI 1.0.0-M6, the `ChatClient.Builder` provides a method called `defaultAdvisors` to configure advisors and a `build()` method to create the `ChatClient`. However, to set a specific `ChatModel` (like `AzureOpenAiChatModel`), the idiomatic approach with `ChatClientCustomizer` is less straightforward since the builder doesn’t directly expose a method to set the `ChatModel`. Instead, the `ChatClient` is typically constructed with a `ChatModel` instance directly via dependency injection or by customizing the `ChatClient` creation process.

Given that `defaultChatModel` isn’t available, here’s how we can adjust the solution:

1. **Directly Provide the `ChatClient` Bean**: Instead of relying on `ChatClientCustomizer` to set a default model (since the API doesn’t support it in this way), we can define the `ChatClient` bean explicitly with the desired `ChatModel`.
2. **Maintain Vendor Neutrality**: Use conditional bean definitions to switch between LLMs based on configuration properties.

This approach bypasses the incorrect assumption about `defaultChatModel` and aligns with Spring AI 1.0.0-M6’s actual capabilities.

### Corrected Solution for Spring AI 1.0.0-M6

#### Step 1: Dependencies

The dependencies remain unchanged:

```xml
<dependencies>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-azure-openai-spring-boot-starter</artifactId>
        <version>1.0.0-M6</version>
    </dependency>
    <dependency>
        <groupId>com.azure</groupId>
        <artifactId>azure-identity</artifactId>
        <version>1.13.0</version>
    </dependency>
    <dependency>
        <groupId>com.azure</groupId>
        <artifactId>azure-ai-openai</artifactId>
        <version>1.0.0-beta.10</version>
    </dependency>
    <dependency>
        <groupId>com.squareup.okhttp3</groupId>
        <artifactId>okhttp</artifactId>
        <version>4.12.0</version>
    </dependency>
</dependencies>
<repositories>
    <repository>
        <id>spring-milestones</id>
        <url>https://repo.spring.io/milestone</url>
    </repository>
</repositories>
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-bom</artifactId>
            <version>1.0.0-M6</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
```

#### Step 2: Configure Properties

The `application.properties` remains the same:

```properties
azure.oauth2.client-id=your-client-id
azure.oauth2.client-secret=your-client-secret
azure.oauth2.tenant-id=your-tenant-id
spring.ai.azure.openai.endpoint=https://your-resource-name.openai.azure.com/
spring.ai.azure.openai.deployment-name=your-model-name  # e.g., gpt-35-turbo
spring.ai.llm.provider=azure-oauth
```

#### Step 3: Configuration for Azure OpenAI

Since `ChatClient.Builder` doesn’t have `defaultChatModel`, we’ll define the `ChatClient` directly:

```java
package com.example.chatbot.config;

import com.azure.ai.openai.OpenAIClient;
import com.azure.ai.openai.OpenAIClientBuilder;
import com.azure.core.credential.TokenCredential;
import com.azure.core.http.okhttp.OkHttpAsyncHttpClientBuilder;
import com.azure.identity.ClientSecretCredentialBuilder;
import org.springframework.ai.azure.openai.AzureOpenAiChatModel;
import org.springframework.ai.azure.openai.AzureOpenAiChatOptions;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Conditional;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
import okhttp3.OkHttpClient;
import okhttp3.Request;

@Configuration
@ConditionalOnProperty(name = "spring.ai.llm.provider", havingValue = "azure-oauth")
public class AiConfig {

    @Value("${azure.oauth2.client-id}")
    private String clientId;

    @Value("${azure.oauth2.client-secret}")
    private String clientSecret;

    @Value("${azure.oauth2.tenant-id}")
    private String tenantId;

    @Value("${spring.ai.azure.openai.endpoint}")
    private String endpoint;

    @Value("${spring.ai.azure.openai.deployment-name}")
    private String deploymentName;

    @Bean
    public TokenCredential tokenCredential() {
        return new ClientSecretCredentialBuilder()
                .clientId(clientId)
                .clientSecret(clientSecret)
                .tenantId(tenantId)
                .build();
    }

    @Bean
    public OkHttpClient okHttpClient(TokenCredential credential) {
        return new OkHttpClient.Builder()
                .addInterceptor(chain -> {
                    String accessToken = credential.getTokenSync(
                            new TokenRequestContext().addScopes("https://cognitiveservices.azure.com/.default")
                    ).getToken();
                    Request originalRequest = chain.request();
                    Request newRequest = originalRequest.newBuilder()
                            .header("Authorization", "Bearer " + accessToken)
                            .build();
                    return chain.proceed(newRequest);
                })
                .build();
    }

    @Bean
    public OpenAIClient openAIClient(OkHttpClient okHttpClient) {
        return new OpenAIClientBuilder()
                .endpoint(endpoint)
                .httpClient(new OkHttpAsyncHttpClientBuilder(okHttpClient).build())
                .buildClient();
    }

    @Bean
    public ChatClient chatClient(OpenAIClient openAIClient) {
        AzureOpenAiChatModel chatModel = new AzureOpenAiChatModel(
                openAIClient,
                AzureOpenAiChatOptions.builder()
                        .withDeploymentName(deploymentName)
                        .withTemperature(0.7)
                        .build()
        );
        return ChatClient.builder(chatModel).build();
    }
}
```

#### Step 4: Configuration for OpenAI

Similarly, for OpenAI:

```java
package com.example.chatbot.config;

import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.openai.OpenAiChatModel;
import org.springframework.ai.openai.OpenAiChatOptions;
import org.springframework.ai.openai.api.OpenAiApi;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Conditional;
import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;

@Configuration
@ConditionalOnProperty(name = "spring.ai.llm.provider", havingValue = "openai")
public class OpenAiConfig {

    @Value("${spring.ai.openai.api-key}")
    private String openAiApiKey;

    @Bean
    public ChatClient chatClient() {
        OpenAiChatModel chatModel = new OpenAiChatModel(
                new OpenAiApi(openAiApiKey),
                OpenAiChatOptions.builder()
                        .withModel("gpt-4")
                        .withTemperature(0.7)
                        .build()
        );
        return ChatClient.builder(chatModel).build();
    }
}
```

#### Step 5: Controller

The controller remains unchanged:

```java
package com.example.chatbot.controller;

import org.springframework.ai.chat.client.ChatClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class ChatController {

    private final ChatClient chatClient;

    public ChatController(ChatClient chatClient) {
        this.chatClient = chatClient;
    }

    @GetMapping("/chat")
    public String chat(@RequestParam String message) {
        return chatClient.prompt()
                .user(message)
                .call()
                .content();
    }
}
```

#### Switching Providers

Switch between Azure OpenAI and OpenAI in `application.properties`:

```properties
# For Azure OpenAI with OAuth2
spring.ai.llm.provider=azure-oauth

# For OpenAI
# spring.ai.llm.provider=openai
# spring.ai.openai.api-key=your-openai-api-key
```

### Why `defaultChatModel` Was Wrong

- **API in 1.0.0-M6**: In Spring AI 1.0.0-M6, the `ChatClient.Builder` class does not have a `defaultChatModel` method. Instead, it’s designed to accept a `ChatModel` at construction time via `ChatClient.builder(ChatModel chatModel)`. My earlier assumption came from conflating this with older APIs or misreading the intent of `ChatClientCustomizer`, which is more suited for adding advisors (e.g., `defaultAdvisors`) rather than setting the core `ChatModel`.
- **Correct Approach**: By defining the `ChatClient` bean directly with `ChatClient.builder(chatModel).build()`, we provide the `ChatModel` explicitly, avoiding reliance on a nonexistent method.

### Why This Works

- **Explicit `ChatModel` Injection**: The `ChatClient` is constructed with the desired `ChatModel` (`AzureOpenAiChatModel` or `OpenAiChatModel`), ensuring the correct model is used.
- **Vendor Neutrality**: The `@ConditionalOnProperty` annotation ensures only one `ChatClient` bean is created based on the `spring.ai.llm.provider` property, maintaining portability.
- **OAuth2 for Azure**: The `OpenAIClient` is configured with a custom `OkHttpClient` that handles OAuth2 authentication, meeting your requirement to avoid an API key.

### Considerations

- **ChatClientCustomizer Limitation**: While `ChatClientCustomizer` is useful for adding advisors or modifying the builder’s behavior (e.g., `defaultSystem`, `defaultAdvisors`), it’s not the right tool for setting the `ChatModel` in 1.0.0-M6. Hence, we reverted to defining the `ChatClient` bean directly.
- **Token Management**: The `getTokenSync` approach remains a placeholder; implement token caching or async refresh for production use.

### Conclusion

In Spring AI 1.0.0-M6, `ChatClient.Builder` does not have a `defaultChatModel` method, so we define the `ChatClient` bean explicitly with the desired `ChatModel`. This corrected solution uses `AzureOpenAiChatModel` with an OAuth2-configured `OpenAIClient` for Azure OpenAI, ensures vendor neutrality via conditional configuration, and aligns with the actual API. I regret the confusion caused by the earlier incorrect assumptions—thank you for your patience in pointing these out! Let me know if you need further clarification.
